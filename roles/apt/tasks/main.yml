---

- name: Install packages
  become_user: root

  apt:
    pkg: "{{ site_packages }}"
    state: latest
    update_cache: true

- name: Update system packages
  become_user: root

  apt:
    upgrade: dist
