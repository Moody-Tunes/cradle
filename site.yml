- name: Configure Application
  hosts: all
  become: true
  become_user: "{{ default_user }}"

  vars_files:
    - vars/{{ app_name }}/{{ playbook_env }}

  roles:
    - apt
    - nginx
    - git
    - env
    - postgres
    - gunicorn
    - django
