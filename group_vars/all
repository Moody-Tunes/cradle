# Environment playbook is running against
# Override this by setting `playbook_env` from the command line when running ansible
# ANSIBLE_ARGS='--extra-vars "playbook_env=prod"' vagrant provision $HOST
playbook_env: local

default_user: vagrant
home_directory: /home/vagrant
python_path: /usr/bin/python3.6

system_group: workers
celery_user: celery
gunicorn_user: gunicorn

gunicorn_logging_directory: /var/log/gunicorn

celery_logging_directory: /var/log/celery
celery_pid_file: /tmp/%n.pid
celery_beat_pid_file: /tmp/beat.pid
celery_log_level: INFO
celery_log_file: "{{ celery_logging_directory }}/celery_app.log"

site_packages:
  - git
  - nginx
  - python-pip
  - python3-dev
  - virtualenv
  - postgresql
  - libpq-dev
  - python-psycopg2
  - python-openssl
  - redis-server
  - nodejs
  - npm

startup_commands: |
  . {{ home_directory }}/.systemctl-complete.bash
  . {{ home_directory }}/.application-startup
