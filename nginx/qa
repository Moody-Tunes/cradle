server {

    location = /favicon.ico { access_log off; log_not_found off; }

    location /static/ {
        alias $STATIC_DIR;
    }

    location / {
        include proxy_params;
        proxy_pass $PROXY_PATH; socket
    }

    listen 80;
    server_name moodytunes.qa www.moodytunes.qa;

}